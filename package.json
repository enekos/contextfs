{
  "name": "mairu",
  "version": "2.0.0",
  "description": "Mairu monorepo: Go coding agent, context server, and TypeScript context engine with dashboard.",
  "license": "ISC",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "bin": {
    "mairu-context": "dist/cli.js",
    "context-cli": "dist/cli.js",
    "ctx": "dist/cli.js"
  },
  "engines": {
    "bun": ">=1"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "mairu:build": "mkdir -p mairu/bin && go build -C mairu -o bin/mairu-agent ./cmd/mairu",
    "mairu:web": "./mairu/bin/mairu-agent web -p 8080",
    "mairu:context-server": "bun run mairu:build && ./mairu/bin/mairu-agent context-server -p 8788",
    "mairu:dashboard": "bun run mairu:build && docker compose up -d meilisearch && bun run mairu:web",
    "typecheck": "tsc --noEmit",
    "setup": "bun mairu/contextfs/src/scripts/setup.ts",
    "eval:retrieval": "bun mairu/contextfs/src/eval/evaluate.ts",
    "eval:seed": "bun mairu/contextfs/src/eval/evaluate.ts --seed true",
    "eval:full": "bun mairu/contextfs/src/eval/evaluate.ts --seed true --cleanup true",
    "eval:ablation": "bun mairu/contextfs/src/eval/evaluate.ts --ablation true",
    "dashboard:api": "bun run mairu:context-server",
    "dashboard:dev": "bun run --cwd mairu/ui dev",
    "dashboard": "bun run --parallel dashboard:api dashboard:dev",
    "dashboard:build": "bun run --cwd mairu/ui build",
    "sync:notion": "bun mairu/contextfs/src/notionSync.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "oxlint mairu/contextfs/src",
    "link": "bun run build && bun link",
    "prepublishOnly": "bun run clean && bun run build"
  },
  "keywords": [
    "agents",
    "meilisearch",
    "context",
    "vector-search",
    "hybrid-search",
    "rag",
    "semantic-search"
  ],
  "author": "",
  "dependencies": {
    "@google/genai": "^1.45.0",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "@mozilla/readability": "^0.6.0",
    "@repomix/tree-sitter-wasms": "^0.1.16",
    "chokidar": "^5.0.0",
    "commander": "^14.0.3",
    "dotenv": "^17.3.1",
    "linkedom": "^0.18.12",
    "meilisearch": "^0.57.0",
    "playwright": "^1.58.2",
    "turndown": "^7.2.2",
    "web-tree-sitter": "^0.26.7"
  },
  "devDependencies": {
    "@types/mozilla-readability": "^0.2.1",
    "@types/node": "^22.0.0",
    "@types/turndown": "^5.0.6",
    "@vitest/coverage-v8": "^2.0.0",
    "oxlint": "^1.56.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
